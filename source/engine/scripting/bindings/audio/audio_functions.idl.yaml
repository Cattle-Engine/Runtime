RegistrationPriority: 90

RequiredCppHeaders:
  - "engine/audio/audio.hpp"
  - "engine/audio/audio_resource_manager.hpp"
  - <cstdint>
  - <string>

ASNamespace: CE::Audio

ASClassFunctions:
  - Name: LoadSound
    ReturnType: void
    Signature: const string& in filepath, const AudioType& in type, AudioAsset& out handle
    CppSignature: const std::string&, const CE::Core::Audio::AudioType&, CE::Audio::Resources::AudioHandle&
    Description: Loads an audio file from the VFS
    Function: |
      if (!mRuntime.mAudioManager) return;
      arg2 = mRuntime.mAudioManager->LoadSound(arg0, arg1);

  - Name: UnloadSound
    ReturnType: void
    Signature: const AudioAsset& in handle
    CppSignature: const CE::Audio::Resources::AudioHandle&
    Description: Unloads an audio handle
    Function: |
      if (!mRuntime.mAudioManager) return;
      mRuntime.mAudioManager->UnloadSound(arg0);

  - Name: CreatePlayingAudio
    ReturnType: void
    Signature: const AudioAsset& in, PlayingAudio& out
    CppSignature: const CE::Audio::Resources::AudioHandle& in, CE::Audio::Resources::PlayingAudioHandle& out
    Description: Creates a PlayingAudio handle
    Function: |
      if (!mRuntime.mAudioManager) return;
      arg1 = mRuntime.mAudioManager->CreateSoundInstance(arg0);

  - Name: IsPlaying
    ReturnType: bool
    Signature: const PlayingAudio& in
    CppSignature: const CE::Audio::Resources::PlayingAudioHandle&
    Description: Returns false if a PlayAudio's audio is playing audio
    Function: |
      if (!mRuntime.mAudioManager) return false;
      return mRuntime.mAudioManager->IsPlaying(arg0);

  - Name: DestroyPlayingAudio
    ReturnType: void
    Signature: const PlayingAudio& in
    CppSignature: const CE::Audio::Resources::PlayingAudioHandle&
    Description: Destroys a PlayingAudio handle. If it is playing audio, it is stopped
    Function: |
      if (!mRuntime.mAudioManager) return;
      mRuntime.mAudioManager->DeleteSoundInstance(arg0);

  - Name: PlaySound
    ReturnType: void
    Signature: const PlayingAudio& in
    CppSignature: const CE::Audio::Resources::PlayingAudioHandle& 
    Description: Plays an audio file from the start
    Function: |
      if (!mRuntime.mAudioManager) return;
      mRuntime.mAudioManager->PlaySound(arg0);

  - Name: PauseSound
    ReturnType: void
    Signature: const PlayingAudio& in
    CppSignature: const CE::Audio::Resources::PlayingAudioHandle& 
    Description: Pauses a playing sound (saves playback position)
    Function: |
      if (!mRuntime.mAudioManager) return;
      mRuntime.mAudioManager->PauseSound(arg0);

  - Name: ResumeSound
    ReturnType: void
    Signature: const PlayingAudio& in
    CppSignature: const CE::Audio::Resources::PlayingAudioHandle& 
    Description: Resumes a playing sound from where playback was last stopped
    Function: |
      if (!mRuntime.mAudioManager) return;
      mRuntime.mAudioManager->ResumeSound(arg0);

  - Name: SeekSound
    ReturnType: void
    Signature: const PlayingAudio& in, float seconds
    CppSignature: const CE::Audio::Resources::PlayingAudioHandle&, float
    Description: Seeks playback in seconds
    Function: |
      if (!mRuntime.mAudioManager) return;
      mRuntime.mAudioManager->SeekSound(arg0, arg1);

  - Name: StopSound
    ReturnType: void
    Signature: const PlayingAudio& in
    CppSignature: const CE::Audio::Resources::PlayingAudioHandle& 
    Description: Stops a playing sound (doesn't save playback position)
    Function: |
      if (!mRuntime.mAudioManager) return;
      mRuntime.mAudioManager->StopSound(arg0);

  - Name: StopAllSounds
    ReturnType: void
    Signature: ""
    CppSignature: ""
    Description: Stops all playing sounds (doesn't save playback position)
    Function: |
      if (!mRuntime.mAudioManager) return;
      mRuntime.mAudioManager->StopAll();

  - Name: PauseAllSounds
    ReturnType: void
    Signature: ""
    CppSignature: ""
    Description: Pauses all playing sounds (saves playback position)
    Function: |
      if (!mRuntime.mAudioManager) return;
      mRuntime.mAudioManager->PauseAll();

  - Name: ResumeAllSounds
    ReturnType: void
    Signature: ""
    CppSignature: ""
    Description: Resumes all paused sounds from the playback position
    Function : |
      if (!mRuntime.mAudioManager) return;
      mRuntime.mAudioManager->ResumeAll();

  - Name: AddEffect 
    ReturnType: void
    Signature: const PlayingAudio& in, const string& in, const AudioEffect& in
    CppSignature: const CE::Audio::Resources::PlayingAudioHandle&, const std::string&, const CE::Core::Audio::AudioEffect&
    Description: Adds an effect to playing audio
    Function: |
      if (!mRuntime.mAudioManager) return;
      mRuntime.mAudioManager->AddEffect(arg0, arg1, arg2);

  - Name: RemoveEffect
    ReturnType: void
    Signature: const PlayingAudio& in, const string& in
    CppSignature: const CE::Audio::Resources::PlayingAudioHandle&, const std::string&
    Description: Removes an effect from playing audio
    Function: |
      if (!mRuntime.mAudioManager) return;
      mRuntime.mAudioManager->RemoveEffect(arg0, arg1);

  - Name: ClearEffects
    ReturnType: void
    Signature: const PlayingAudio& in
    CppSignature: const CE::Audio::Resources::PlayingAudioHandle&
    Description: Clears all effects from playing audio
    Function: |
      if (!mRuntime.mAudioManager) return;
      mRuntime.mAudioManager->ClearEffects(arg0);

  - Name: SetSoundBus
    ReturnType: void
    Signature: const PlayingAudio& in, const string& in bus_name
    CppSignature: const CE::Audio::Resources::PlayingAudioHandle&, const std::string& 
    Description: Sets a playing audio's sound bus
    Function: |
      if (!mRuntime.mAudioManager) return;
      mRuntime.mAudioManager->SetSoundBus(arg0, arg1);

  - Name: GetSoundBus
    ReturnType: string
    CppReturnType: std::string
    Signature: const PlayingAudio&
    CppSignature: const CE::Audio::Resources::PlayingAudioHandle&
    Description: Gets a PlayingAudio's sound bus name
    Function: |
      if (!mRuntime.mAudioManager) return "";
      return mRuntime.mAudioManager->GetSoundBus(arg0);

  - Name: SetBusVolume
    ReturnType: void
    Signature: const string& in bus_name, float volume
    CppSignature: const std::string& in, float
    Description: Sets a bus volume
    Function: |
      if (!mRuntime.mAudioManager) return;
      mRuntime.mAudioManager->SetBusVolume(arg0, arg1);

  - Name: SetBusVoiceLimit
    ReturnType: void
    Signature: const string& in bus_name, uint limit
    CppSignature: const std::string& in, uint32_t limit
    Description: Sets how many audio instances can play on that bus
    Function: |
      if (!mRuntime.mAudioManager) return;
      // Maybe a unsafe conversion on some platforms?
      mRuntime.mAudioManager->SetBusVoiceLimit(arg0, static_cast<size_t>(arg1));

  - Name: SetSoundVolume
    ReturnType: void
    Signature: const PlayingAudio& in, int volume
    CppSignature: const CE::Audio::Resources::PlayingAudioHandle&, int
    Description: Sets a playing audio volume
    Function: |
      if (!mRuntime.mAudioManager) return; 
      mRuntime.mAudioManager->SetSoundVolume(arg0, arg1);

  - Name: SetMasterVolume
    ReturnType: void
    Signature: float
    CppSignature: float
    Description: Sets the global volume
    Function: |
      if (!mRuntime.mAudioManager) return;
      mRuntime.mAudioManager->SetMasterVolume(arg0);

  - Name: SetMusicVolume
    ReturnType: void
    Signature: float
    CppSignature: float
    Description: Sets the music volume
    Function: |
      if (!mRuntime.mAudioManager) return;
      mRuntime.mAudioManager->SetMusicVolume(arg0);

  - Name: SetSFXVolume
    ReturnType: void
    Signature: float
    CppSignature: float
    Description: Sets the music volume
    Function: |
      if (!mRuntime.mAudioManager) return;
      mRuntime.mAudioManager->SetSFXVolume(arg0);

  - Name: SetSoundMuted
    ReturnType: void
    Signature: const PlayingAudio& in, bool muted
    CppSignature: const CE::Audio::Resources::PlayingAudioHandle&, bool
    Description: Mutes or unmutes a sound
    Function: |
      if (!mRuntime.mAudioManager) return; 
      mRuntime.mAudioManager->SetSoundMuted(arg0, arg1);

  - Name: SetSoundGain
    ReturnType: void
    Signature: const PlayingAudio& in, float gain
    CppSignature: const CE::Audio::Resources::PlayingAudioHandle&, float
    Description: Sets a sounds gain
    Function: |
      if (!mRuntime.mAudioManager) return; 
      mRuntime.mAudioManager->SetSoundGain(arg0, arg1);

  - Name: SetSoundLabel
    ReturnType: void
    Signature: const PlayingAudio& in, const string in label
    CppSignature: const CE::Audio::Resources::PlayingAudioHandle&, const std::string&
    Description: Sets a sound label
    Function: |
      if (!mRuntime.mAudioManager) return;
      mRuntime.mAudioManager->SetSoundLabel(arg0, arg1);

  - Name: GetSoundLabel
    ReturnType: string
    Signature: const PlayingAudio& in
    CppSignature: const CE::Audio::Resources::PlayingAudioHandle&
    Description: Get a sound label
    Function: |
      if (!mRuntime.mAudioManager) return "";
      mRuntime.mAudioManager->GetSoundLabel(arg0);